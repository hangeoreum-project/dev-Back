spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${MYSQL_DB:tour2025}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PWD:gunhamster@1945}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create / update / validate / none 중 선택 가능
    show-sql: true
    properties:
      hibernate:
        format_sql: true